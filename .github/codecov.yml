coverage:
  status:
    project:
      default:
        target: '80%'
        threshold: '0.5%'
        if_not_found: 'success'
    patch:
      default:
        target: '100%'
        informational: false

ignore:
  - 'cmd/**'